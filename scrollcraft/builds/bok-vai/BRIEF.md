# BRIEF — bok-vai

Status: interview in progress. Step 0 answers below are verbatim from the
human, including items explicitly deferred to AI judgment. Step 1 (subject
framing, belief, CTA) still needs confirmation before act planning.

## Step 0 — Interview (verbatim)

1. **Vibe / references**: "film" — no named references given. Reads as: lean
   toward a cinematic, filmic treatment (see grammar options — filmic one-shot
   is one candidate, not the default) rather than a specific mood word set.
2. **Scroll journey, in their words**: "the app on a phone, discription
   [description], app working" — visitor sees the app on a phone, gets a
   description of what it does, then sees the app actually working
   (a real interaction, not a static shot).
3. **Energy curve**: "Ai choice here" — deferred. Claude decides in Step 2,
   written into the feeling curve before the score table.
4. **Feeling stage-by-stage / the one peak moment**: "the app" — the
   through-line is the app itself; the peak should be the moment the app
   visibly does its job (idea in, BUILD/KILL/PIVOT verdict out).
5. **Signature move seed**: "Make you want to use the app" — the bespoke
   interaction's job is to create download intent, not just demonstrate a
   feature.
6. **Aesthetic lane**: "premium-minimal".
7. **One world vs. distinct scenes**: "your choice" — deferred. Claude decides
   in Step 2 and states why.
8. **Assets already owned**: real brand kit and product screenshots, pulled
   from https://github.com/TorQ-code/BoK-Build-or-Kill.git (cloned to
   `/home/user/bok-build-or-kill`):
   - Logo: VAi wordmark (electric-cyan gradient stroke, small orange spark),
     `attached_assets/VAI_logo_1773322841114.jpg`
   - App icon: BOK wordmark on deep-navy rounded square,
     `attached_assets/BOK_icon_1776434566792.png`
   - Palette: Electric Cyan `#00E5FF`, Deep Navy `#0A1428`,
     Vibrant Orange `#FF4D00`, Deep Violet `#4C1D95`, Pure White `#F8F9FA`
   - Dozens of real in-app screenshots under `attached_assets/` (Overview,
     Market TAM/SAM/SOM, Rivals, Money, Build tabs; score ring; splash/paywall
     exports in `exports/`)
   - No footage/video of the app in use yet — screens are static screenshots,
     so "app working" (journey beat 3) likely needs either a screen-recording
     supplied by the human or a generated/simulated scroll-driven UI sequence.
   This is a real-assets build: no KIE_AI_API_KEY spend needed for stills;
   still confirm before generating anything.

## Subject (confirmed by human: "This is pre-order page.")

BOK ("Build or Kill") — validates a startup idea in ~60 seconds via AI,
returns a BUILD / KILL / PIVOT verdict with market analysis, competitor
breakdown, monetization strategy, risk flags, and a 90-day roadmap.
Freemium: 3 free validations, then Pro via IAP ($19/mo or $99/yr).
Audience: early-stage founders / indie hackers with an idea they haven't
validated yet.

**This is a pre-order page, not a "download now" page.** The app is not yet
live for these visitors — the single action is committing ahead of launch
(pre-order / reserve / get-notified), not opening an app store. That reframes:
- Beat 3 ("app working") is a preview/demo of what pre-ordering unlocks, not
  a live download CTA.
- Belief to install: leans toward "this will actually tell me the truth
  about my idea, and I want to be first in line for it" rather than "I use
  this today."
- CTA label: something in the pre-order/reserve family — exact wording still
  open (see below).

**Confirmed by human:**
- CTA label, used everywhere on the page: **"Reserve your spot."**
- Belief to install: *"This will actually tell me the truth about my idea,
  and I want to be first in line for it."*

## The journey (Step 1)

Five beats, built from interview answer 2 ("the app on a phone, description,
app working") expanded to carry a belief and end on the confirmed CTA:

```
1  Recognition   the app itself, on a phone — no preamble, the product first
2  Tension       what it costs to build on an idea nobody's stress-tested
3  Turn          the app working: an idea goes in, a real BUILD/KILL/PIVOT
                 verdict comes out — the moment the promise becomes visible
4  Substance     why the verdict holds up — real market/competitor/roadmap
                 analysis, not a vibe check (Money/Build tab material)
5  Commitment    reserve your spot — the one action, pre-launch
```

Beat 3 is the peak candidate: it's where "the app" (interview Q4) and "make
you want to use it" (Q5, the signature-move seed) converge.

**Open asset question, not yet answered by the human:** beat 3 needs the app
visibly working. The source repo has real static screenshots but no screen
recording of the app running live. Two ways to get a true "it's working"
moment without inventing a fake product: (a) the human supplies a real screen
capture, or (b) build the sequence from the real screenshots themselves
(idea-input → analysis → verdict reveal), driven by scroll/CSS rather than
generated video. Defaulting to (b) — real assets, no synthetic app UI, no
kie.ai spend — unless told otherwise.

## Feeling curve, peak, tell-someone sentence, silence

Not yet written — depends on Step 2 (grammar, signature move, fingerprint
gate) which comes after Step 1 is confirmed.
